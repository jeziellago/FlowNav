jdk:
  - openjdk8
before_install:
   - cd sample; chmod +x gradlew 
install:
   - ./gradlew :annotation:assemble
   - ./gradlew :processor:assemble
   - ./gradlew :router:assembleRelease
   - ./gradlew publishToMavenLocal  
   - ls ~/.m2/repository/com/github/jeziellago/FlowNav
